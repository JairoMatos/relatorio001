window.sr = ScrollReveal({ reset: true});
sr.reveal('h2', { 
    duration: 1500,
    rotate: {x:0, y:80, z:0}
});

sr.reveal('i', {
    rotate: {x:0, y:80, z:0},
    duration: 1000,

});

let check = document.querySelector('.selector');